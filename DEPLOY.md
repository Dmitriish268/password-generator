# 🚀 Инструкция по деплою на GitHub

## Создание репозитория

1. Создайте новый репозиторий на GitHub:
   - Перейдите на https://github.com/new
   - Введите название репозитория (например: `password-generator`)
   - Выберите Public или Private
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

## Загрузка кода

### Через командную строку:

```bash
# Перейдите в папку проекта
cd "путь/к/проекту"

# Инициализируйте git репозиторий
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Password Generator"

# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/password-generator.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

### Через GitHub Desktop:

1. Откройте GitHub Desktop
2. File → Add Local Repository
3. Выберите папку проекта
4. Publish repository
5. Введите название и описание
6. Нажмите Publish

## Настройка GitHub Pages (опционально)

1. Перейдите в Settings вашего репозитория
2. В разделе Pages:
   - Source: выберите `Deploy from a branch`
   - Branch: выберите `main` и папку `/ (root)`
   - Нажмите Save
3. Через несколько минут ваш сайт будет доступен по адресу:
   `https://YOUR_USERNAME.github.io/password-generator/`

## Обновление README

Не забудьте заменить в README.md:
- `yourusername` на ваш GitHub username
- Добавить реальные ссылки на Issues и другие разделы

## Дополнительные настройки

### Добавление тегов (опционально):

```bash
git tag -a v1.0.0 -m "First release"
git push origin v1.0.0
```

### Добавление описания репозитория:

На странице репозитория нажмите ⚙️ (Settings) и добавьте:
- Описание: "Современный генератор паролей с расчетом времени взлома"
- Topics: `password-generator`, `security`, `javascript`, `html`, `css`

